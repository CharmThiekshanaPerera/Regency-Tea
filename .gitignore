/vendor
/node_modules
/public/build
/public/media
/public/hot
/public/storage
/storage/*.key
/storage/framework/cache/*
/storage/framework/sessions/*
/storage/framework/views/*
/storage/logs/*
.env
.env.backup
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.idea
/.vscode

# Never commit the legacy backup or old DB credentials
/_backup
wp-config.php

# Stray copy at the repo root — the repo root isn't served by the app at
# all (Laravel's web root is /public), so this one does nothing. The real,
# working copy that Google actually needs stays at /public/googleaeb2c150e29a1cd8.html.
/googleaeb2c150e29a1cd8.html
